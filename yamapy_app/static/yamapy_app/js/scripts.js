let typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    loop: true,
    typeSpeed: 50,
    backDelay: 700,
    backSpeed: 30,
});

window.scrollTo(0, document.body.scrollHeight);

